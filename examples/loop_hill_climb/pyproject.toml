[project]
name = "loop-hill-climb"
version = "0.1.0"
description = "Demo: loop cell hill climbing on Himmelblau's function"
requires-python = ">=3.12"
dependencies = [
    "pyarrow>=18.0.0",
    "orjson>=3.10.0",
    "cloudpickle>=3.0.0",
]

[tool.uv]
